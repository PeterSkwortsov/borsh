/** @type {import('next').NextConfig} */
// init Swiper:

const nextConfig = {
    output: 'export',
    trailingSlash: true,
    images: {
        unoptimized: true
    },
    fancybox: true
    // init Swiper:
  
}

export default nextConfig;
