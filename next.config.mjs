/** @type {import('next').NextConfig} */
const nextConfig = {
    images: { unoptimized: false },
    // output: 'export',
};

export default nextConfig;

// const nextConfig = {
//     output: 'export',
// };